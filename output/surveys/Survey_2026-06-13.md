# Survey — Polis · 2026-06-13
Specs scanned: 24 (feature 20 · reference 4 · architecture 0 standalone — the architecture map lives in `reference/`)
Verdict: 3 findings (Drift 2 · Undocumented 1 · Unimplemented 0 · Untested 0)

> **Resolved 2026-06-13 (same day):** all 3 findings reconciled (spec-only edits, code was
> correct). llm-system_spec — `VALID_FACTION_ACTIONS` gained `Toil`, `ENTRENCH_NARRATIVE`/`floor`
> removed, stale `translation.py` path corrected, status note added. game-ui_spec — "The People"
> block documented in the Mayor Window feature. Recommendation 3 (design-doc status of
> llm-system) addressed via the new status note. See commit.

Scope note: this was a project-wide static pass. Deep behavioural reads on the core/governance
specs (mayor, treasury, projects, llm-system, game-ui, personality-system) and the full
public-needs cluster I shipped this session (public-needs + the 6 specs it edited + the 3
reference docs). The stable audience/setup satellites (player-identity, faction-descriptions,
audience-deal-card, tax-exemption-shelve) got a read + test-backing check; llm-profiles,
audience-training-log, audience-build-target-info got a structural/test-backing check by their
named test files. Test suite was green at **437 passing** going in (not run by surveyor — that's
inspector's job; cited as context).

## Spec ↔ code map
| Spec (class) | Code area | Result |
|---|---|---|
| cycle-runner (feature) | engine/cycle/runner.py, resolution.py, end_of_cycle.py | clean (item 5b + toiling verified) |
| actions (feature) | engine/actions/faction.py, cycle/resolution.py | clean (Toil present) |
| faction-behavior (feature) | engine/npc/behavior.py, weights.py, targeting.py | clean (Toil weights present) |
| personality-system (feature*) | engine/models.py (FactionTrait), npc/behavior.py | clean — *no Done-when (design doc)* |
| public-needs (feature) | engine/needs/{bands,chain,drift}.py | clean (this session's build) |
| mayor (feature) | engine/mayor/actions.py, api/schemas.py | clean (roster verified vs VALID_MAYOR_ACTIONS) |
| treasury (feature) | engine/mayor/treasury.py, engine/formulas.py | clean |
| projects (feature) | engine/projects/processing.py, models.py (BaseProjectStack) | clean |
| audience (feature) | engine/llm/audiences.py, prompt_builder.py, response_parser.py | clean (Toil term + needs line present) |
| special-factions (feature) | engine/special/{public,moneylender,external_threats}.py | clean (ThePublic fields present) |
| events (feature) | engine/events/event_system.py, world.py, cascades.py | clean (need-gates present) |
| llm-system (feature*) | engine/llm/* | **2 drift items** (see below) |
| llm-profiles (feature) | engine/llm/crypto.py, api/routes/llm_profiles.py | test-backed (test_llm_profiles) |
| player-identity (feature) | engine/llm/prompt_builder.py, db/models.py, api/routes/sim.py | clean |
| faction-descriptions (feature) | engine/models.py, serializer.py, data/factions.json | clean |
| game-ui (feature) | frontend/src/views/GameView.vue, DashboardView.vue, components/* | **1 undocumented panel** (see below) |
| audience-deal-card (feature) | frontend/src/components/AudienceModal.vue | clean (frontend-only) |
| audience-training-log (feature) | engine/llm/audience_log.py | test-backed (test_audience_training_log) |
| audience-build-target-info (feature) | engine/llm/prompt_builder.py, projects | test-backed (test_audience_terms, test_base_project_description) |
| tax-exemption-shelve (feature) | engine/llm/prompt_builder.py, MayorActionsModal.vue | clean (dormant path intact) |
| data-models (reference) | engine/models.py | clean (domains corrected this session) |
| formulas (reference) | engine/formulas.py, engine/needs/* | clean (needs constants added this session) |
| architecture (reference) | (system map) | clean (needs module added this session) |
| theming (reference) | data/factions.json, prompt_builder.py | not re-verified (content/flavor source) |

\* *personality-system and llm-system are filed in `specs/` but carry no `**Done when:**` items — see Recommendations.*

## Drift
| Spec · section | Spec says | Code does | Recommend |
|---|---|---|---|
| llm-system · Response Parser → Validation (line 297) | `VALID_FACTION_ACTIONS = {"BuildProject", "Protect", "Grow"}` | `response_parser.py:22` = `{"BuildProject", "Protect", "Grow", "Toil"}` — Toil added by public-needs (audience_spec + parser were updated; this spec was not) | Fix the spec (architect) — add Toil |
| llm-system · State Narrative (lines 154-159, 244) | `ENTRENCH_NARRATIVE` dict + prompt skeleton "Organisationally you are [entrench narrative]" and "floor [N]"; banner claims the state-narrative assembly "remains accurate" | No `entrench`, `floor`, or `ENTRENCH_NARRATIVE` anywhere in `engine/llm/` — both removed in the demo-redesign; prompt_builder uses HEALTH_NARRATIVE + RATING_NARRATIVE only | Fix the spec (architect) — drop the entrench/floor narrative, or trim llm-system to its still-accurate core |

## Undocumented
| Code | What it does | Recommend |
|---|---|---|
| `GameView.vue` "The People" section (Mayor-window grid: population, Fed band, Mood/drunk, Health/sickly) — added in public-needs final slice | Surfaces the Public-needs state in the play UI | Fix the spec — add a "The People" block to `game-ui_spec.md`'s Mayor Window feature. public-needs_spec covers the *behavior* and has a human-required "needs read clearly" criterion, but game-ui_spec is the structural home for play-screen panels and currently lists only Treasury/AP/Reputation/World |

## Recommendations
1. **[MEDIUM]** Reconcile `llm-system_spec.md` — it has accreted drift across two redesigns (entrench/floor removal, then Toil). It's a large, half-superseded spec already pointing most of its prompt sections at newer specs (player-identity, faction-descriptions, audience). Run **architect** to either (a) add Toil to `VALID_FACTION_ACTIONS`, drop the entrench/floor narrative, and re-verify the parser-validation block against `response_parser.py`, or (b) demote it to a thin layer-architecture doc and let audience/player-identity/faction-descriptions own the prompt truth (which they already do, with tests).
2. **[LOW]** Document the "The People" UI panel in `game-ui_spec.md` (small architect edit to the Mayor Window feature) so the play screen's structure is fully specced.
3. **[LOW]** `personality-system_spec.md` and `llm-system_spec.md` sit in `specs/` but carry no `**Done when:**` items — they read as design docs. Their *behaviour* is tested via the specs that delegate to or supersede them (faction-behavior, audience, player-identity, faction-descriptions). Consider noting their design-doc status explicitly, or reclassifying, so a future surveyor doesn't flag them as untestable feature specs.

No **Unimplemented** items (no spec'd-but-unbuilt behaviour found) and no **Untested automated criteria** (every automated Done-when traced in the deep-read specs maps to a committed test; suite green at 437). Sections with no findings are omitted per format.
