# Survey — Polis · 2026-05-29
Specs scanned: 20 (feature 17 · reference 3 · architecture 1)
Verdict: 6 findings (Drift 2 · Unimplemented 1 · Undocumented 2 · Untested 1) + 1 structural note

**Scope note:** deep spec↔code read was done for the 5 feature specs that carry `**Done when:**`
items (audience, faction-descriptions, player-identity, theme-conversion, game-ui) plus targeted
checks of known drift spots. The 12 criteria-less descriptive specs got a structural/spot pass,
not a line-by-line read — see the structural note.

## Spec ↔ code map
| Spec (class) | Code area | Result |
|---|---|---|
| audience_spec (feature, 13 auto/4 human) | engine/llm/ | Backed — all 13 automated items have committed tests |
| faction-descriptions_spec (7 auto/3 human) | models, serializer, loaders, prompt_builder, GameView, AudienceModal | Backed (test_faction_descriptions) |
| player-identity_spec (12 auto/1 human) | db SimRun, prompt_builder, sim route, TitleView | Backed (test_player_identity) |
| theme-conversion_spec (12 auto/1 human) | data/, db/seed | Mostly backed (test_theme_data, test_seed_official); 5 grep/CLI items unpinned — see Untested |
| game-ui_spec (0 auto/16 human) | frontend/ | Human-required only — no tests expected |
| actions_spec (feature, no DW) | engine/actions/ | Tested (test_actions, test_harm_block_steal) but no acceptance criteria — see structural note |
| cycle-runner_spec (no DW) | engine/cycle/ | Tested (test_cycle, test_npc_and_eoc); no DW |
| events_spec (no DW) | engine/events/ | Tested (test_events, test_events_system); no DW |
| faction-behavior_spec (no DW) | engine/npc/ | Tested (test_npc_and_eoc); no DW |
| mayor_spec (no DW) | engine/mayor/ | Tested (test_mayor, test_mayor_act); no DW |
| treasury_spec (no DW) | engine/mayor/treasury | Tested (test_mayor); no DW |
| projects_spec (no DW) | engine/projects/, data/projects.json | Mechanics tested (test_projects); **data drift** — see Drift |
| special-factions_spec (no DW) | engine/special/ | Tested (test_special_factions); no DW |
| personality-system_spec (no DW) | engine/models, npc | Partial coverage; no DW |
| llm-system_spec (no DW) | engine/llm/ | **Prompt-structure section stale** — see Drift |
| llm-profiles_spec (no DW) | engine/llm/crypto, api | Tested (test_llm_profiles, test_llm_crypto); no DW |
| city-generation_spec (no DW, FUTURE) | — (hand-authored data) | Intentionally unimplemented — see Unimplemented |
| data-models.md (reference) | engine/models, serializer | Aligned (budget_allocation removed this session) |
| formulas.md (reference) | engine/formulas | Aligned (not deep-checked) |
| theming.md (reference) | data/ | Domains/factions aligned; **projects.json not** — see Drift |
| docs/architecture.md | engine/ modules | Lightly checked; modules listed still exist |

## Drift
| Spec · section | Spec says | Code does | Recommend |
|---|---|---|---|
| theming.md (one canonical Greek theme, 8 domains) / theme-conversion | All content uses the 8 Greek domains | `backend/data/projects.json` uses legacy domains `arcane, city_watch, commons, docks, noble_houses, registry, temple, underworld` — the pre-Greek set | Fix the data (re-theme projects) under the Change rules |
| llm-system_spec · "Full Prompt Structure" / "City Setting / Tone" | Prompt opens with a setting tone note; player is "Mayor"; no leader personality or faction description in the prompt | Prompt opens with the canonical Greek briefing; player is the "Prytanis"; leader personality + faction description are injected (governed now by player-identity & faction-descriptions specs) | Fix the spec (architect) — reconcile llm-system_spec or point its prompt section at the newer specs |

## Unimplemented
| Spec · section | Behavior | Status | Recommend |
|---|---|---|---|
| city-generation_spec (whole) | Procedural city generation (domains/factions/world from generators) | Intentionally deferred — header marks it FUTURE; cities are hand-authored JSON | Open question — leave as a clearly-marked future spec, or move it out of `specs/` until scheduled |

## Undocumented
| Code | What it does | Recommend |
|---|---|---|
| frontend Wine-Dark restyle (`style.css` + component colors) | Recolors the whole UI to the Polis palette | Acceptable (visual styling); optionally point a one-line reference at `docs/Polis Design/` |
| Leader personality in the audience prompt + AudienceModal (`prompt_builder.leader_note`, `AudienceModal.leaderLine`) | Surfaces the leader's `personality_notes` in the LLM prompt and the audience window | Add a line to faction-descriptions_spec, or accept as a minor extension |

## Untested automated criteria
| Spec · Done-when | Recommend |
|---|---|
| theme-conversion · `validate_state.py passes` (L62), `Rivers_Point/` absent (L79), grep-`Rivers`-in-backend (L80), grep-`Planning/specs`+README legacy terms (L116), README/city-gen doc assertions (L117) | Accept — these are one-time *migration* acceptance checks (grep/CLI/filesystem), already verified; not behavior to pin with a unit test. Optionally fold the grep checks into one small test. |

## Recommendations
1. **[HIGH]** Re-theme `backend/data/projects.json` to the 8 Greek domains (and refresh the seeded/loaded projects). This is the most visible coherence gap — legacy domain names show in the live Projects panel. Content/flavor only; do not change project mechanics.
2. **[MEDIUM]** Reconcile `llm-system_spec.md` with the current prompt — its "Full Prompt Structure" predates the Greek briefing, the Prytanis rename, and the leader/faction description injection. Run **architect** to update it or explicitly delegate the prompt-structure truth to player-identity / faction-descriptions / audience.
3. **[MEDIUM — framework coherence]** Structural: 12 of 17 feature specs carry no `**Done when:**` items (actions, city-generation, cycle-runner, events, faction-behavior, llm-profiles, llm-system, mayor, personality-system, projects, special-factions, treasury). The engine *is* well-tested (12+ engine test files), but those tests don't trace to acceptance criteria. For a repo that showcases the Plumbline convention, consider an **architect** pass to add Done-when to the core specs — or reclassify the purely descriptive ones as design/reference docs so the absence is intentional, not drift.
4. **[LOW]** Decide city-generation_spec's status (future spec vs. move out of `specs/`).
5. **[LOW]** Optionally document the leader-personality prompt addition and the UI restyle.
