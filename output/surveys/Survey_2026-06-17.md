# Survey — Polis (core-logic specs) · 2026-06-17
Specs scanned: 16 feature specs (core-logic focus) + reference docs cross-checked.
Verdict: **3 findings** (Drift 0 · Unimplemented 1 · Undocumented 1 · Untested 1 cluster)

Static pass (walkthrough drift phase). The four newest features (withhold, piety+unrest,
consumption, confidence) were freshly built+inspected this month and verified against code here as
a side effect — no drift in their specs. Focus below is the **older** core-logic specs.

## Spec ↔ code map (core-logic)
| Spec (feature) | Code area | Result |
|---|---|---|
| actions_spec | engine/actions/, cycle/resolution.py | agrees ("Seven actions" incl. Toil/Withhold; dispatch present) |
| faction-behavior_spec | engine/npc/behavior.py | agrees (BASE_WEIGHTS, trait/state modifiers incl. Toil/Withhold/unrest/confidence) |
| cycle-runner_spec | engine/cycle/runner.py | agrees on order (incl. 5a active-events-before-needs); **test gaps** (below) |
| treasury_spec | engine/mayor/treasury.py | agrees; **1 undocumented field** (below) |
| mayor_spec | engine/mayor/ | agrees (forced-outcome tests back it) |
| projects_spec | engine/projects/ | agrees |
| audience_spec | engine/llm/ | agrees (needs-line incl. drunk/sickly still valid) |
| special-factions_spec | engine/special/, models.py | entity agrees; **1 unimplemented section** (below) |
| food-supply_spec | engine/needs/chain.py | agrees (compute_chain, efficiency, wine_happy) |
| public-needs_spec | engine/needs/drift.py, scales.py, bands.py | agrees (all 7 scales) |
| events_spec | engine/events/event_system.py | agrees (band gates, public-targeted effects) |
| personality/llm-system/llm-profiles/player-identity/faction-descriptions/game-ui | respective | not deeply scanned (presentation/LLM tier; lower drift risk) |

## Unimplemented
| Spec · section | Behavior | Status | Recommend |
|---|---|---|---|
| special-factions_spec · "The Public's Role in Faction Politics" (L113–124) | Disposition-driven faction effects: `content` → distrusts-Mayor factions −5 Steal + Mayor decree −1 AP; `restless` → Broker-DC +2; `angry` → aggressive factions +10 Harm | **Not in code** — `behavior.py` has no `disposition`/`content` logic. AND now **superseded**: the confidence posture modifier (public-needs_spec, 2026-06-17) covers the same intent (public mood → faction aggression) with a cleaner 5-band model over `support`. | **Fix the spec (architect):** mark this section superseded by the Confidence posture modifier, or, if the decree-AP / Broker-DC sub-effects are still wanted, re-spec them as confidence-band consequences. Do not silently keep an unimplemented section. |

## Undocumented
| Code | What it does | Recommend |
|---|---|---|
| `Treasury.guard_paid_this_cycle` (models.py) + its set in `process_treasury_step0` | Cycle flag: true iff the full guard payroll cleared; read by the unrest City-Guard lever (public-needs_spec) | **Fix the spec (architect):** add one line to treasury_spec noting the flag is set at step 0 and consumed by the unrest Guard lever. Small. |

## Untested automated criteria
| Spec · Done-when | Recommend |
|---|---|
| cycle-runner · initiative re-rolled every cycle / "no persistent initiative advantage" (stated twice) | **Add test** — no test references `initiative_order`; the property is unproven. |
| cycle-runner · `faction_actions` count excludes `Skip` | **Add test** — current test asserts `>= 0` (tautological); the Skip-exclusion clause is never exercised. |
| cycle-runner · treasury step runs before the action loop; its results appear in CycleResult | **Add test** — only the treasury-absent path is covered; ordering + results-presence is not. |
| cycle-runner · `utilization = Σ level`, no stale carryover | **Add test** — current assertion is `utilization >= 0` (vacuous). |
| cycle-runner · `build_actions_this_cycle` reset through `run_cycle` | **Add test** — proven only by calling `tick_projects` directly, not end-to-end. |

(Detail + the 3 tautological `test_cycle.py` tests are in `output/walkthrough/InspectorAudit_2026-06-17.md`.)

## Recommendations
1. **[MEDIUM]** Reconcile special-factions "Role in Faction Politics" — unimplemented + superseded by Confidence. Architect call (Full-Path).
2. **[LOW]** Document `Treasury.guard_paid_this_cycle` in treasury_spec. Architect, small.
3. **[MEDIUM]** Harden `test_cycle.py` — replace the 3 tautological assertions and add the 5 cycle-runner orchestration tests above. Adding tests is **Quick-Path** → done in this walkthrough's Phase 3.
