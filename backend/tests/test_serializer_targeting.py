"""
test_serializer_targeting.py — v3 stub. Serializer and unit targeting removed.

Placeholder — serializer will be rewritten for v3 when API layer is updated.
"""
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

import pytest


class TestPlaceholder:
    def test_placeholder(self):
        """Serializer tests pending v3 API rewrite."""
        assert True
