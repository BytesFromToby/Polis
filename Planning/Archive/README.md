# Archive

Retired docs kept for history, out of the way of current truth:
- **Shipped proposals** — a built feature's design doc (doubles as its decision record).
- **Superseded specs** — a spec replaced or made obsolete (e.g. a completed one-time migration).

Nothing here is authoritative. Current behavior lives in `../specs/`; current as-built truth in
`../reference/`.
