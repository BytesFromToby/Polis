# City_Sim backend dependencies
# Install with: pip install -r requirements.txt

# Web API
fastapi>=0.135,<1.0
uvicorn>=0.43,<1.0

# Database
SQLAlchemy>=2.0,<3.0

# Validation
pydantic>=2.12,<3.0

# Auth & crypto
python-jose>=3.5,<4.0
passlib>=1.7,<2.0
bcrypt>=4.0,<5.0
cryptography>=46.0

# Testing
pytest>=8.0
httpx>=0.27,<1.0   # required by FastAPI's TestClient in the API tests

# Optional LLM providers — needed only for live faction audiences.
# The engine runs with a built-in stub client if these are absent.
# Uncomment the provider you want to use:
# anthropic>=0.40
# openai>=1.40
